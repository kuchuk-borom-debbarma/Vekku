package dev.kbd.vekku_server.controllers.rest;

import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/v1/tag")
@Slf4j
@RequiredArgsConstructor
public class TagController {
    //1. endpoint to create tag
    //2. endpoint to delete tag
    //3. endpoint to update tag
    //4. endpoint to get tags paginated
    // 5. endpoint to get tag by id
    // 6. endpoint to search tags by name
    // 7. endpoint to search tags by synonym
}
