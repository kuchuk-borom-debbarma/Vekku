# Keycloak Configuration
keycloak.auth-server-url=http://localhost:8180
keycloak.realm=vekku
keycloak.admin-username=admin
keycloak.admin-password=admin
# Spring Security OAuth2 Resource Server
spring.security.oauth2.resourceserver.jwt.issuer-uri=${keycloak.auth-server-url}/realms/${keycloak.realm}
# Database Configuration (Postgres)
spring.datasource.url=jdbc:postgresql://localhost:5432/vekku
spring.datasource.username=vekku
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Brain Service Configuration (REST)
brain-service.url=http://localhost:3000

# ==========================================
# 5. Rate Limiting (Bucket4j)
# ==========================================

# ==========================================
# 6. CORS Configuration
# ==========================================
app.cors.allowed-origins=http://localhost:5173,http://localhost:5174,http://localhost:3000

# ==========================================
# 7. RabbitMQ Configuration
# ==========================================
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

vekku.rabbitmq.queue=content.process.queue
vekku.rabbitmq.exchange=content.exchange
vekku.rabbitmq.routingkey=content.routing.key

# ==========================================
# 8. Spring AI & Vector Store (Qdrant)
# ==========================================
spring.ai.vectorstore.qdrant.host=localhost
spring.ai.vectorstore.qdrant.port=6334
spring.ai.vectorstore.qdrant.collection-name=suggestion_vectors